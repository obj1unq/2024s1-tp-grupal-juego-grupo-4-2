import wollok.game.*
import froggi.*

program juego {
	game.width(20)
	game.height(10)
	game.title("Juego Base")
	game.addVisualCharacter(froggi)
	keyboard.c().onPressDo({ froggi.actualizarEstado() })
	game.start()
}