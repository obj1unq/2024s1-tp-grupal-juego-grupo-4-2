import froggi.*

describe "personaje"{
	//PERSONAJE
	const froggi = new Froggi()
	
	test "froggi cambia estado a grande" {

	assert.equals(froggi.estado() , chiquito)
	froggi.actualizarEstado()
	assert.equals(froggi.estado() , grande)
	}
}
